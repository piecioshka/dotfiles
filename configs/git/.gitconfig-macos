[debug]
    os = macos

[delta]
    syntax-theme = Monokai Extended
    navigate = true
    color-only = true
    line-numbers = true

[interactive]
    diffFilter = delta --color-only

[pager]
    log = delta
    diff = delta
    show = delta
    status = delta
    reflog = delta
